# 智能投资组合优化与分析 Agent - 依赖列表
# 仅使用国内可用 API（多重备份）

# 数据获取（三数据源备份）
akshare==1.18.39      # 主要数据源（最稳定）
efinance>=0.1.0       # 第一备用（东方财富直接源）
baostock>=0.8.9       # 第二备用（历史数据/财报）

# 可选备用（特殊需求）
# qstock>=1.3.8       # A 股 + 可视化（需要额外安装 backtrader）
# finshare>=1.1.0     # 轻量 K 线（API 可能已变更）

# 数据处理
pandas==2.2.0
numpy==1.26.3

# 可视化
matplotlib==3.8.3
plotly==5.19.0

# 优化算法
scipy==1.12.0

# Jupyter Notebook（可选）
jupyter==1.0.0
ipywidgets==8.1.1

# 工具
requests==2.31.0
tqdm==4.66.1
